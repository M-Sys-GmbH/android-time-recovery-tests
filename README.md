# android-time-recovery-tests
ADB‑based Android time recovery test runner that forces bad system time, verifies offline persistence, and validates automatic correction via Wi‑Fi, mobile data, and reboot — with JSON &amp; HTML reports.
